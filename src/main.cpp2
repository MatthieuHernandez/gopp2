#include "io.h2"
#include "engine.h2"

main: () -> int = {
    exit: bool = false;
    printMenu();
    do {
        selection: int = getInputMenu();
        exit = inspect selection -> bool {
            is 1 = :()->bool = {
                clear();
                game: Game = ();
                game.play();
                return false;
            }();
           is 2 = :()->bool={
            clear("Bye.");
            return true;
        }();
            is _ = :()->bool = {
                print("Invalid selection.");
                return false;
            }();
        };
    } while !exit;
  return 0;
}
