#include "engine.h2"
#include "move.h2"
#include "io.h2"

Game : type = {
    play : () = {
        engine : Engine = ();
        printGoban();
        current_player: = Color::Black;
        do {
            move: = GetInputMove(current_player);
            engine.playMove(x);
            state = engine.moves;
            printGoban(state);
        switchPlayer(current_player);
        } while !game.isFinish
}
}